[project]
name = "a2a-reference-test"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "a2a-sdk[http-server]==1.1.2",
    "genai-reference-shared",
    "uvicorn==0.35.0",
]

[tool.uv.sources]
genai-reference-shared = { path = "../../shared", editable = true }

[tool.uv]
package = false
