Add `gen_ai.agent.interaction.type`, distinguishing agent delegation from handoff, to the `gen_ai.execute_tool` and `gen_ai.invoke_agent` spans and the `gen_ai.execute_tool.duration` metric, and state that the agent-execution metrics (`gen_ai.invoke_agent.duration`, `gen_ai.invoke_agent.inference_calls`, `gen_ai.invoke_agent.tool_calls`) are not recorded for the caller-owned delegation or handoff operation.
