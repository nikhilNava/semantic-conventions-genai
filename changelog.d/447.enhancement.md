Define tool-based agent transfer as the `gen_ai.execute_tool.transfer.internal` refinement of `gen_ai.execute_tool.internal`, with a target-qualified span name when the transfer target is available at span creation and the generic execute-tool name as a fallback, while protocol-based remote invocation uses `gen_ai.invoke_agent.client`.
