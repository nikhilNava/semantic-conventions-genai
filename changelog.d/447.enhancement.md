Add `gen_ai.agent.interaction.type`, distinguishing agent delegation from handoff, to caller-owned `gen_ai.execute_tool` and `gen_ai.invoke_agent` client operations and the `gen_ai.execute_tool.duration` metric, and clarify the corresponding agent-to-agent trace shapes.
